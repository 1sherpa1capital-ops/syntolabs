{
    "framework": "vite",
    "buildCommand": "cd landingpage && bun run build",
    "outputDirectory": "landingpage/dist",
    "installCommand": "cd landingpage && bun install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}
